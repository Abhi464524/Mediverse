import 'package:flutter/material.dart';

/// Central color palette for the entire app.
/// Change values here to update colors globally.
class AppColors {
  AppColors._();

  // Primary (Sage Green)
  static const Color primary = Color(0xFF6A9C89);
  static const Color primaryLight = Color(0xFFC4DAD2);
  static const Color primaryBg = Color(0xFFF8FBF9);
  static const Color primarySurface = Color(0xFFF2F7F5);

  // Emergency (Red)
  static const Color emergency = Color(0xFFFFC0C0);
  static const Color emergencyLight = Color(0xFFFFCDD2);
  static const Color emergencyBg = Color(0xFFFFF5F5);
}
